import { FilterTycketByIdPipe } from './filter-ticket-by-id.pipe';

describe('FilterTicketByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTycketByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
